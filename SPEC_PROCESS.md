# SPEC_PROCESS — 过程文档

> 记录 ScholarAgent 项目 Brainstorming 关键节点、设计迭代、以及冷启动验证结果。

---

## 1. Brainstorming 关键节点

### 第一轮：领域定义

**输入**：AI4SE 期末项目 Track A — Coding Agent Harness  
**讨论**：什么领域最适合展示 Harness 的 6 个维度（决策封装、工具、记忆、治理、反馈、配置）？  
**结论**：学术文献综述自动化（ScholarAgent）

**理由**：
- 信息可信度要求高 → 反馈闭环和守卫系统自然必要
- 反馈信号客观明确 → 校验器可纯代码实现
- 危险操作边界清晰 → 操作安全守卫有价值
- 记忆需求分层 → 会话/持久双层级记忆合理

### 第二轮：架构设计

**讨论**：
- 单体 vs 前后端分离 → 选择前后端分离（FastAPI + React）
- 自编码 vs 第三方框架 → 自编码 Harness（满足课程要求）
- 状态机 vs 线性流程 → 状态机（支持中断/恢复/重试）

**关键决策**：
1. 5 状态主循环：PLANNING → RETRIEVAL → ANALYSIS → WRITING → VALIDATION → COMPLETE
2. 反馈闭环作为重点维度（§A.4-D）
3. Mock-LLM 测试策略确保所有机制可脱离真实 LLM 验证

### 第三轮：反馈闭环深化

**讨论**：如何让反馈闭环满足"机制是代码"的要求？
**结论**：5 个校验器全部用确定性代码实现，不依赖 LLM 判断
- `check_citations`：正则匹配 `[@id]` 引用标记
- `detect_hallucination`：检测 `[citation-needed]` 标记
- `check_word_count`：分词计数
- `polish_language`：正则匹配非正式表达
- `check_coherence`：正则匹配过渡词

---

## 2. 冷启动验证

### 什么是冷启动验证

冷启动验证是指在项目正式开发前，对 SPEC 中的核心假设进行快速验证，确保设计可行。

### 验证 1：状态机转换路径

**假设**：状态机转换路径完整覆盖所有场景  
**验证方法**：手写状态转换表，模拟所有合法和非法路径  
**结果**：✅ 发现 `FEEDBACK` 状态被状态机定义但未出现在主循环路径中，保留作为扩展点

### 验证 2：校验器边界条件

**假设**：5 个校验器能处理所有边界输入  
**验证方法**：为每个校验器设计极端输入（空字符串、超长字符串、特殊字符）  
**结果**：✅ 所有校验器对空输入返回 `not passed` 而非崩溃

### 验证 3：Mock-LLM 测试可行性

**假设**：Mock-LLM 足以驱动 Harness 状态转换  
**验证方法**：设计 `test_harness_integrates_feedback_loop` 测试用例  
**结果**：✅ 通过 mock feedback 可完全验证多轮迭代逻辑

---

## 3. 设计迭代记录

### 迭代 1：初始设计

- 简单线性 Agent 流程
- 无守卫系统
- 无反馈闭环

### 迭代 2：加入治理

- 增加 5 类守卫（来源过滤、事实绑定、操作安全、速率限制、输出标准化）
- 守卫绑定到工具执行链路

### 迭代 3：加入反馈闭环（最终设计）

- 增加 5 个确定性校验器
- 增加反馈聚合器 + 修复生成器
- 增加多轮迭代控制
- 将反馈集成到 Harness 主循环

---

## 4. 风险缓解

| 风险 | 缓解措施 | 状态 |
|------|---------|------|
| LLM 输出质量不稳定 | 反馈闭环捕获质量缺陷并触发修正 | ✅ 已实现 |
| Google Scholar 反爬 | 作为最后备选，控制频率 | ✅ 已设计 |
| PDF 解析质量差 | 降级为摘要级分析 | ✅ 已设计 |
| 上下文长度限制 | 分章节写作 + 独立验证 | ✅ 已设计 |

---

## 5. 交付物状态

| # | 交付物 | 状态 |
|---|--------|------|
| 1 | SPEC.md | ✅ 已完成 |
| 2 | PLAN.md | ✅ 已完成 |
| 3 | SPEC_PROCESS.md | ✅ 本文件 |
| 4 | 完整源代码 | ✅ 已完成 |
| 5 | Dockerfile | ✅ 已完成 |
| 6 | README.md | ✅ 已完成 |
| 7 | AGENT_LOG.md | ✅ 已完成 |
| 8 | CI 配置 | ✅ 已完成 |
| 9 | 70 个测试全部通过 | ✅ 已验证 |
| 10 | REFLECTION.md | ✅ 已完成 |