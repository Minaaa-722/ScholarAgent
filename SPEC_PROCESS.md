# SPEC_PROCESS — 过程文档

> 记录 ScholarAgent 项目 Brainstorming 关键节点、设计迭代、以及冷启动验证结果。

---

## 1. Brainstorming 关键节点

### 第一轮：领域定义

**输入**：AI4SE 期末项目 Track A — Coding Agent Harness  
**讨论**：什么领域最适合展示 Harness 的 6 个维度（决策封装、工具、记忆、治理、反馈、配置）？  
**结论**：学术文献综述自动化（ScholarAgent）

**理由**：
- 信息可信度要求高 → 反馈闭环和守卫系统自然必要
- 反馈信号客观明确 → 校验器可纯代码实现
- 危险操作边界清晰 → 操作安全守卫有价值
- 记忆需求分层 → 会话/持久双层级记忆合理

### 第二轮：架构设计

**讨论**：
- 单体 vs 前后端分离 → 选择前后端分离（FastAPI + React）
- 自编码 vs 第三方框架 → 自编码 Harness（满足课程要求）
- 状态机 vs 线性流程 → 状态机（支持中断/恢复/重试）

**关键决策**：
1. 5 状态主循环：PLANNING → RETRIEVAL → ANALYSIS → WRITING → VALIDATION → COMPLETE
2. 反馈闭环作为重点维度（§A.4-D）
3. Mock-LLM 测试策略确保所有机制可脱离真实 LLM 验证

### 第三轮：反馈闭环深化

**讨论**：如何让反馈闭环满足"机制是代码"的要求？
**结论**：5 个校验器全部用确定性代码实现，不依赖 LLM 判断
- `check_citations`：正则匹配 `[@id]` 引用标记
- `detect_hallucination`：检测 `[citation-needed]` 标记
- `check_word_count`：分词计数
- `polish_language`：正则匹配非正式表达
- `check_coherence`：正则匹配过渡词

---

## 2. 设计自验证

### 什么是设计自验证

设计自验证是指在项目正式开发前，对 SPEC 中的核心假设进行快速验证，确保设计可行。这是由设计者本人完成的初步验证。

### 验证 1：状态机转换路径

**假设**：状态机转换路径完整覆盖所有场景  
**验证方法**：手写状态转换表，模拟所有合法和非法路径  
**结果**：✅ 发现 `FEEDBACK` 状态被状态机定义但未出现在主循环路径中，保留作为扩展点

### 验证 2：校验器边界条件

**假设**：5 个校验器能处理所有边界输入  
**验证方法**：为每个校验器设计极端输入（空字符串、超长字符串、特殊字符）  
**结果**：✅ 所有校验器对空输入返回 `not passed` 而非崩溃

### 验证 3：Mock-LLM 测试可行性

**假设**：Mock-LLM 足以驱动 Harness 状态转换  
**验证方法**：设计 `test_harness_integrates_feedback_loop` 测试用例  
**结果**：✅ 通过 mock feedback 可完全验证多轮迭代逻辑

---

## 3. 陌生智能体冷启动试运行

> 本节对应通用要求 §4.5。使用与主开发智能体（Claude Code）不同的编码智能体，在全新会话中仅凭 SPEC + PLAN 尝试实现 1-2 个 task，以验证规约的清晰度与完整性。
>
> **测试智能体**：OpenAI Codex CLI（与主开发工具 Claude Code 不同类型）
> **测试时间**：2026-08-10
> **提供材料**：SPEC.md + PLAN.md（仅文件，无任何口头补充说明）
> **选取 task**：PLAN 中的 Task 1（Harness 核心主循环框架）和 Task 3（OpSafety 守卫实现）

### 3.1 验证过程记录

#### Task 1：Harness 核心主循环框架

**agent 行为摘要**：
1. 读取 SPEC.md 后，agent 在 2 分钟内理解了整体架构，开始读取 PLAN.md 中 Task 1 的具体描述。
2. 在实现 `state.py` 的状态机时，agent 暂停并提问：**"状态转换表中 `FEEDBACK` 状态在哪些场景下会触发？从哪些状态可以进入 FEEDBACK？"**
   - 这是一个有价值的 spec 缺陷信号：SPEC 中定义了 FEEDBACK 状态，但未在状态转换表中明确其触发条件与转换路径。SPEC 修订：在 §3.1 状态机定义中补充了 FEEDBACK 的触发条件（校验器 FAIL 且重试次数未超限）。
3. 在实现 `Harness.__init__` 时，agent 暂停并提问：**"工具注册表（ToolRegistry）是否需要在初始化时预注册所有工具，还是支持延迟注册？如果支持延迟注册，`run` 方法在调用未注册工具时应该抛异常还是返回错误结果？"**
   - 这一暂停暴露了 SPEC 中未定义的问题。SPEC 修订：在 §3.2 工具系统中补充了延迟注册机制和错误处理策略。

**产出质量**：生成的 `harness.py`、`state.py`、`llm.py` 核心结构与已有实现基本一致，但存在以下偏差：
- agent 自行决定将 `MockLLM` 放在 `llm.py` 内部而非独立的测试文件中（与已有实现不一致）
- 状态机实现使用了 `if-elif` 链而非显式转换表（风格差异，功能等价）

#### Task 3：OpSafety 守卫实现

**agent 行为摘要**：
1. agent 先读取了 SPEC §3.4（守卫系统）和 PLAN 中 Task 3 的描述。
2. 在实现拦截逻辑时，agent 暂停并提问：**"当守卫拦截一个危险动作后，应当向调用者返回什么？是抛出异常、返回特殊值、还是调用一个回调函数？"**
   - 这一暂停暴露了 SPEC 中守卫接口定义不完整的问题。SPEC 在 §3.4 中定义了守卫的输入（action 对象）和输出（拦截/放行），但未明确**拦截后的控制流**——即谁来处理拦截结果。SPEC 修订：在 §3.4 中补充了 `GuardrailVerdict` 枚举（PASS / BLOCK / REQUIRE_APPROVAL）和调用者处理协议。
3. agent 实现了一个正则匹配模式列表，但列表中的模式与已有实现不完全一致（缺少 `format c:` 和 `DROP DATABASE` 模式）。

**产出质量**：生成的守卫核心逻辑正确，但存在以下问题：
- 危险命令模式列表不完整（缺少 2 个模式）
- 未实现 `REQUIRE_APPROVAL` 的暂停机制，仅返回了 `BLOCK`
- 未编写单元测试（与 TDD 要求不符）

### 3.2 暴露的 SPEC 缺陷

| # | 缺陷 | 发现场景 | 修订措施 |
|---|------|---------|---------|
| 1 | FEEDBACK 状态缺少触发条件与转换路径 | Task 1 agent 暂停提问 | 在 SPEC §3.1 补充状态转换表 |
| 2 | 工具注册表未定义延迟注册与错误处理策略 | Task 1 agent 暂停提问 | 在 SPEC §3.2 补充注册机制 |
| 3 | 守卫拦截后的控制流未定义 | Task 3 agent 暂停提问 | 在 SPEC §3.4 补充 GuardrailVerdict 枚举 |
| 4 | 守卫的模式列表无基线定义 | Task 3 实现偏差 | 在 PLAN 中给出危险模式列表的基线 |

### 3.3 解读不一致的分析

| 不一致点 | 已有实现 | 冷启动 agent 的解读 | 原因分析 |
|---------|---------|-------------------|---------|
| MockLLM 位置 | `tests/` 目录 | `agent/core/llm.py` 内部 | PLAN 未明确文件路径 |
| 状态机实现 | 显式转换表 | if-elif 链 | SPEC 未约束实现方式 |
| 守卫返回值 | GuardrailVerdict 枚举 | 简单布尔值 | SPEC 未定义返回类型 |
| 单元测试编写 | 先写测试 | 未写测试 | PLAN 中未强调 TDD 要求 |

### 3.4 对 SPEC / PLAN 的修订

**修订 1（SPEC §3.1）**：状态转换表补充 FEEDBACK 状态的完整定义

```
修订前：FEEDBACK 状态出现在状态列表中，但转换表中未定义其转换路径。
修订后：在转换表中增加 FEEDBACK → WRITING（重试次数未超限时）和 FEEDBACK → COMPLETE（重试次数超限时）。
```

**修订 2（SPEC §3.4）**：守卫接口补充 GuardrailVerdict 枚举

```
修订前：守卫的 `check` 方法返回是否拦截，类型未定义。
修订后：守卫的 `check` 方法返回 `GuardrailVerdict`（PASS / BLOCK / REQUIRE_APPROVAL），
        并明确调用者（工具执行器）根据 verdict 决定控制流。
```

**修订 3（PLAN Task 3）**：补充守卫模式列表基线和 TDD 要求

```
修订前：Task 3 描述为"实现 OpSafety 守卫"。
修订后：明确危险命令模式列表（rm -rf, drop table, format c: 等），
        并添加"测试先行"的约束条件。
```

### 3.5 反思

冷启动验证暴露了 4 个 SPEC 缺陷和 4 处解读不一致，其中 3 个缺陷是"接口契约未定义"类——即 SPEC 描述了"做什么"但未定义"怎么做"的协议。这印证了 §4.5 的核心论点：设计者与主 agent 在 brainstorming 阶段积累的隐性上下文，会严重高估 spec 的清晰度。

最有价值的发现是**守卫拦截后的控制流**问题——设计者（我）默认"守卫拦截后调用者会处理"，但 SPEC 中从未明确这一协议。对于一个新 agent 而言，这是一个合理的暂停点。如果不是冷启动验证，这个缺陷会在实现阶段才被发现，导致返工。

**改进建议**：在未来的项目中，冷启动验证应在 PLAN 完成后立即执行，而非在开发后期。早期发现的 spec 缺陷可以低成本修复，后期发现的缺陷需要修改已有代码，成本更高。

---

## 4. 设计迭代记录

### 迭代 1：初始设计

- 简单线性 Agent 流程
- 无守卫系统
- 无反馈闭环

### 迭代 2：加入治理

- 增加 5 类守卫（来源过滤、事实绑定、操作安全、速率限制、输出标准化）
- 守卫绑定到工具执行链路

### 迭代 3：加入反馈闭环（最终设计）

- 增加 5 个确定性校验器
- 增加反馈聚合器 + 修复生成器
- 增加多轮迭代控制
- 将反馈集成到 Harness 主循环

---

## 5. 风险缓解

| 风险 | 缓解措施 | 状态 |
|------|---------|------|
| LLM 输出质量不稳定 | 反馈闭环捕获质量缺陷并触发修正 | ✅ 已实现 |
| Google Scholar 反爬 | 作为最后备选，控制频率 | ✅ 已设计 |
| PDF 解析质量差 | 降级为摘要级分析 | ✅ 已设计 |
| 上下文长度限制 | 分章节写作 + 独立验证 | ✅ 已设计 |

---

## 6. 交付物状态

| # | 交付物 | 状态 |
|---|--------|------|
| 1 | SPEC.md | ✅ 已完成 |
| 2 | PLAN.md | ✅ 已完成 |
| 3 | SPEC_PROCESS.md | ✅ 本文件 |
| 4 | 完整源代码 | ✅ 已完成 |
| 5 | Dockerfile | ✅ 已完成 |
| 6 | README.md | ✅ 已完成 |
| 7 | AGENT_LOG.md | ✅ 已完成 |
| 8 | CI 配置 | ✅ 已完成 |
| 9 | 70 个测试全部通过 | ✅ 已验证 |
| 10 | REFLECTION.md | ✅ 已完成 |