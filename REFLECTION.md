# REFLECTION — 项目反思报告

> 约 2300 字 · 对本项目设计与开发过程的全面反思
>
> **说明**：本报告由学生本人撰写，AI 辅助润色（标注了具体润色范围）。

---

## 1. 项目概览

ScholarAgent 是一个自编码的 Coding Agent Harness，用于自动生成 IEEEtran 格式的学术文献综述论文。项目覆盖决策封装、工具、记忆、治理、反馈、配置六个维度，反馈闭环作为重点深入维度。

---

## 2. Superpowers 方法论整体评价

### 2.1 最大作用的技能

**`brainstorming` 技能**价值最大。它通过结构化追问，迫使我在没有看到代码之前明确了"做什么"和"为什么做"。它对四类机制（动作/工具、反馈信号、危险动作、记忆）的系统性追问，直接对应了 §A.3 的要求，让我在 spec 阶段就完成了关键设计决策。

**`subagent-driven-development` 工作流**是项目最核心的执行方式。每个 task 派遣一个新鲜 subagent，在独立 worktree 中完成，天然实现关注点隔离。subagent 的"无记忆"特性既是优点（不会把上一个 task 的上下文污染到当前 task）也是缺点（每次需在 prompt 中重建上下文）。

**`finishing-a-development-branch` 技能**的 merge 决策流程逐条检查验收标准，捕获了"测试通过但功能不完整"情况。

### 2.2 形式大于实质的技能

**`writing-plans`**产出的 PLAN.md 粒度不均。部分 task（如 Web UI 的 5 个页面）被合并为单个，导致 subagent 无法在单次会话中完成。这并非技能问题，而是我对"每个 task 2-5 分钟"指导原则理解不足。

**`requesting-code-review` 技能**的部分检查项在 solo 项目中缺乏对照标准，形式化程度较高。最有价值的评审是**人工指定 reviewer 关注具体风险点**，而非泛化检查。

---

## 3. SDD（Subagent-Driven Development）工作流体验

### 3.1 SDD 的核心模式

项目全程使用 Superpowers 的 SDD 工作流：**SPEC 定义做什么 → PLAN 定义怎么做 → subagent 负责实现 → review 验证质量**。每个 subagent 是"无状态"的——只看到当前 task 的上下文，不携带历史记忆。

### 3.2 SDD 的优缺点

**优点**：
- **关注点隔离**：每个 subagent 只关注一个 task，不会因"知道"其他模块细节而产生意外耦合。如守卫系统，subagent 只需理解 `Guardrail` 接口和 `GuardrailVerdict` 枚举，不需知道状态机或反馈系统如何工作。
- **并行潜力**：多个 subagent 可在独立 worktree 中并行执行。项目中守卫系统和记忆系统的开发就是并行进行的。
- **可复现性**：给定相同的 SPEC/PLAN 和 prompt，不同 subagent 产出的结果高度一致。

**缺点**：
- **上下文重建成本**：每个 subagent 都需要在 prompt 中重新描述项目背景、文件结构、编码约定，导致 prompt 越来越长，且部分信息在传递中丢失。
- **缺乏跨 task 连贯性**：Task 6 和 Task 7 的 subagent 对同一数据结构的理解可能不同，如上文 §5.2 中 `ValidationResult` 的案例。
- **无法利用历史经验**：subagent 不会从之前的错误中学习，同一类错误可能反复出现。

### 3.3 SDD 与 TDD 的关系

课程要求 TDD 是硬性要求（§3.6），但 SDD 的"先 spec、再 plan、再 subagent 实现"流程与 TDD 的"先写失败测试"存在内在张力。subagent 的自然倾向是直接生成实现代码。我尝试过在 prompt 中要求 subagent 先写测试，但效果不稳定。

**我的处理方式**：在 review 阶段补写测试。每个 task 完成后，由我编写测试用例，确保覆盖核心路径和边界条件。这种方式虽不满足 TDD 的"先红再绿"要求，但保证了测试的完整性。项目共 70 个测试，全为 mock-LLM 驱动的确定性测试。

**反思**：如果重做，我会在 PLAN 中为每个 task 明确写出测试用例列表，让 subagent 在实现前先根据这些测试编写代码。这样既保持 SDD 优势，又满足 TDD 纪律。

---

## 4. Subagent 工作流与 Task 拆分

### 4.1 Subagent 能自主运行多久？

- **简单 task（单一文件、明确接口）**：可自主完成。如 `OpSafety` 守卫的实现，一次提交通过。
- **中等 task（2-3 个文件）**：60-70% 进度处需要介入，通常因对现有代码结构的理解偏差。如 `FeedbackAggregator` 实现中，subagent 对 `ValidationResult` 的数据结构假设与已有代码不一致。
- **复杂 task（跨模块、需设计决策）**：30-40% 处偏离轨道。最典型的案例是 Web UI 的 AgentExecution 页面，subagent 自行设计了与 API 不匹配的状态轮询方案。

### 4.2 最优 Task 颗粒度

**"一个文件的核心修改 + 其对应的测试文件"**，预计耗时 10-15 分钟。PLAN.md 中部分 task 试图让 subagent 同时修改 4-5 个文件，这些 task 几乎都需要人工修正。

---

## 5. SPEC/PLAN 质量对实现的影响

### 5.1 正面案例：守卫系统

守卫系统的 SPEC 最为清晰——每个守卫的输入、判定逻辑、输出枚举都有明确定义。subagent 实现时几乎零偏差，一次通过评审。

### 5.2 反面案例：反馈闭环

校验器（Validator）与聚合器（Aggregator）被拆为独立 task，但两者的接口定义在实现中频繁调整，导致返工。

**具体案例**：在 Task 6 中，subagent 为每个校验器设计了不同的 `ValidationResult` 结构。当 Task 7 的新 subagent 开始时，不得不花大量时间理解 5 种不同的输出格式，最终由我介入统一了数据类。这一偏差的根源是 PLAN 中未明确"校验器输出格式必须统一"——如果 PLAN 中写明了数据类定义，偏差本可以避免。

### 5.3 修订后的策略

项目后期采用了 **"SPEC 定义接口契约，PLAN 定义数据流"**：在 PLAN 中为跨 task 共享的数据结构给出明确的代码片段，subagent 的偏差率显著下降。

---

## 6. 最有效的 Prompt/Context 策略

最有效的策略是**结构化 prompt 模板**，包含：任务目标、涉及文件列表、接口契约、验收标准、约束条件。这降低了 subagent 的"探索成本"——它不需要在庞大代码库中自行推断上下文。特别是**接口契约**和**约束条件**，直接减少了接口不匹配和副作用修改这两种最常见的偏差。

---

## 7. 凭据与分发的工程要求

### 7.1 凭据安全

如果没有这一要求，我可能会直接使用 `os.environ["API_KEY"]`。课程要求迫使我想清楚了：存储层选择（环境变量 vs 钥匙串的权衡）、首次运行引导设计、查看 key 时不回显明文、以及威胁模型。

### 7.2 分发

Docker 分发要求让我必须考虑：多阶段构建（减少镜像体积）、非 root 用户（降低容器逃逸风险）、健康检查（运行时状态可观测）、环境变量注入（运行时传 key 而非构建时写入）。这些本可能被忽略的生产环境问题，因分发要求而被纳入设计。

---

## 8. 对 Superpowers 方法论的批判

### 8.1 核心假设

Superpowers 假设**规范化流程可系统性提升 AI 协作产出质量**。这一假设大部分成立——先 spec 再 plan 再实现确实减少了返工，worktree 隔离机制让并行开发成为可能。但有两个假设不完全成立：

- **"技能即流程"**：技能不能替代工程判断。`writing-plans` 可以产出结构化 task 列表，但颗粒度是否合适、依赖是否合理，仍需人工判断。
- **"subagent 可独立执行 task"**：subagent 在简单 task 上表现出色，但一旦涉及跨模块上下文理解，偏差率显著上升。subagent 更适合"实现"而非"设计"。

### 8.2 方法论盲区

Superpowers 对以下场景覆盖不足：非确定性调试（缺乏根因分析引导）、性能优化（缺乏"先测量、再优化"的工作流）、技术债务管理（缺乏决策框架）。

### 8.3 在本项目中的适用性

对于 Coding Agent Harness 这一命题，Superpowers 的方法论整体合适。Harness 的每个机制（守卫、校验器、状态机）都有明确的输入/输出边界，非常适合 subagent 的"实现"模式。但涉及跨机制交互时，人工协调仍不可替代。

---

## 9. 如果重做，我会改变什么

1. **Task 拆分更细**：Web UI 的 5 个页面拆为独立 task。
2. **在 PLAN 中嵌入数据类定义**：跨 task 共享的数据结构给出明确的代码片段。
3. **更早建立 Prompt 模板**：在项目初期就建立结构化 prompt 模板。
4. **冷启动验证提前到 PLAN 完成后立即执行**：让 spec 缺陷在早期被修复。
5. **为前端 task 指定更严格的技术栈约束**：减少 subagent 自由度，保持代码风格一致。

---

## 11. TDD 流程偏离反思

### 11.1 偏离原因

项目初始化时（2026-07-20），我未仔细阅读课程作业的 AI4SE 通用要求文档，未注意到 §3.6 强制要求全开发流程执行 TDD。项目初始设置中，`.claude/settings.json` 未配置全局 `test-driven-development` 强制开关，导致所有历史开发任务均采用 **SPEC → PLAN → 业务实现代码 → 事后补充测试** 的不合规流程，而非标准 TDD 的 **RED → GREEN → REFACTOR** 循环。

Superpowers 将 `test-driven-development` 单独拆分为一个独立技能是有其设计目的的：该技能在 subagent 被调用时自动注入 TDD 约束，确保即使主流程（如 `subagent-driven-development`）本身不强制 TDD，subagent 仍会先写测试后写代码。如果项目初始化时开启了全局强制开关，subagent 的行为会有根本性不同——它们会在实现代码之前产出测试用例，而非之后。

### 11.2 事后补测的工程缺陷

事后补测虽然在"测试数量"上达到了要求（387 个测试），但存在以下根本性缺陷：

1. **测试贴合现有实现**：事后补测容易"为代码写测试"而非"为需求写测试"。测试用例倾向于覆盖代码已经处理的情况，而非代码应该处理但未处理的情况。这在证据层（evidence/）的测试中尤为明显——测试覆盖了现有代码路径，但未发现缺少的边界检查。

2. **遗漏反向边界 case**：由于测试是在实现后编写的，测试者的思维已被实现代码的路径所局限。例如，`CitationStore.register()` 的测试覆盖了正常注册和缺失字段异常，但未测试"重复注册同一篇论文"的幂等性行为——因为实现代码本身就没有处理这种情况。

3. **前期缺少行为约束**：没有测试先行，实现代码缺少"行为规格说明书"。在传统 TDD 中，RED 阶段的测试用例本质上是对需求的精确编码——它们明确了"什么算通过"。缺失这一约束，实现代码的边界模糊，后期需要通过大量集成测试来弥补。

### 11.3 TDD 补救方案

针对上述缺陷，本次执行了以下补救：

1. **全覆盖测试补全**：新增 119 个测试，覆盖先前缺失的证据层引用/表格模块、辅助工具、凭据 API。总测试数从 387 提升至 506。
2. **TDD_RECOVERY.md 流程补证**：为每个模块还原 RED→GREEN→REFACTOR 三阶段流程，明确"应当先写什么测试"。
3. **PLAN.md 修正**：为所有 13 个任务添加 TDD 验证步骤，明确每个任务的 RED/GREEN/REFACTOR 阶段。
4. **CLAUDE.md 配置**：写入全局 TDD 强制规则，禁止先写代码后补测试。

### 11.4 SDD 顶层规约设计 + TDD 单任务校验的互补关系

Superpowers 的 SDD（Subagent-Driven Development）和 TDD（Test-Driven Development）存在自然的互补关系：

- **SDD 负责顶层规约**：SPEC 定义"做什么"，PLAN 定义"怎么做"，subagent 负责"实现"。SDD 的优势在于关注点隔离——每个 subagent 只看到一个 task，不会产生跨模块耦合。
- **TDD 负责单任务校验**：RED 阶段的测试用例定义"做对了没有"，GREEN 阶段的最小实现确保"只做需要的"，REFACTOR 阶段保证"代码质量"。

二者搭配的效果是：SDD 保证了**方向的正确性**（spec 层），TDD 保证了**执行的质量**（实现层）。缺少 TDD，SDD 产生的代码虽然方向正确，但边界质量不可控；缺少 SDD，TDD 只保证单个 task 的质量，但 task 之间的接口一致性无法保证。

课程强制要求二者搭配使用，是因为两者的互补正是 AI 开发流程的弱点——AI 在"方向把握"上容易偏离（需 SDD 的 spec 约束），在"细节精度"上容易遗漏（需 TDD 的测试约束）。

### 11.5 若重做项目的改进点

若重做项目，初始化阶段应做以下操作：

1. **阅读课程要求后立即开启全局 TDD**：在 `.claude/settings.json` 中配置全局 `test-driven-development` 强制开关，确保所有 subagent 任务自动注入 TDD 约束。
2. **在 PLAN.md 中为每个 task 嵌入测试用例列表**：不只是在 PLAN 中写"实现什么"，而是明确写出"预计有哪些测试用例"，让 subagent 在实现前先看到测试规格。
3. **在 AGENT_LOG 中标准化 TDD 执行记录**：每个 task 完成后记录 RED/GREEN/REFACTOR 各阶段的执行情况，而非仅记录"测试通过"。
4. **在 CI 中增加 TDD 合规检查**：在 CI 中增加 step，检查是否每个业务模块都有对应的测试文件，避免出现"代码已实现但测试缺失"的情况。

### 11.6 流程偏离对项目质量的影响

本次流程偏离对项目造成以下负面影响：

1. **过程文档完整性受损**：AGENT_LOG 中缺少 TDD 执行记录，无法向评审者展示"先写测试再写代码"的完整流程证据。TDD_RECOVERY.md 虽然补证了流程，但属于事后还原，效力低于原始 TDD 执行记录。
2. **测试质量低于 TDD 标准**：虽然事后补测达到了 506 个测试，但这些测试更多是"验证现有实现"而非"驱动设计"。测试的"行为规格"价值没有充分发挥。
3. **代码的可测试性设计不足**：TDD 天然推动代码的模块化和松耦合设计（因为紧耦合的代码难以在 RED 阶段写测试）。事后补测缺少这一驱动，部分模块（如 pipeline.py）的代码结构复杂、测试困难。

---

## 12. 总结

三个关键收获：

1. **确定性机制是 harness 的基石**：用代码实现的守卫、校验器、状态机，让系统的大部分行为可预测、可测试。移除 LLM 后，系统仍有 506 个可独立运行的确定性测试。

2. **流程纪律是 AI 协作的放大器**：spec 先行、代码评审、worktree 隔离这些在传统工程中行之有效的纪律，在 AI 协作中同样重要——而且因为 AI 的"无记忆"特性，它们比人工协作时更需要严格执行。SDD 工作流的好处是它强制了这些纪律，但代价是每次 subagent 调用都需要重建上下文。

3. **方法论的边界**：Superpowers 提供了优秀的流程脚手架，但它不能替代工程判断。task 拆分、规格定义、评审标准——这些"做什么"和"做对了吗"的问题，仍需人来回答。

---

*反思报告完 · 2026-08-13*

> **AI 辅助润色说明**：本文第 2、8 节的部分段落经 AI 辅助润色（语言流畅度和段落结构优化），核心观点和案例均为作者本人提出。