# REFLECTION — 项目反思报告
约 2200 字 · 对本项目设计与开发过程的全面反思
> 说明：本报告由学生本人撰写，AI 辅助润色（优化行文结构措辞）

---

## 1. 项目概览
ScholarAgent 是自研 Coding Agent Harness，可自动生成 IEEEtran 格式文献综述。项目分为决策封装、工具、记忆、安全治理、反馈闭环、配置六大模块，反馈闭环为核心深挖模块。

---

## 2. Superpowers 方法论整体评价
### 2.1 收益最高的技能
1. **brainstorming**：编码前通过结构化追问锁定需求、架构、数据模型，完美匹配 Harness 项目设计要求，大幅减少后期返工。
2. **subagent-driven-development**：单任务派发独立无状态子智能体，搭配 git worktree 隔离开发。优点是模块解耦；缺点是每次需重复灌入上下文，存在信息损耗。
3. **finishing-a-development-branch**：按 SPEC 逐条验收，多次拦截“测试通过但不合需求”的缺陷。

### 2.2 落地局限性较强的技能
1. **writing-plans**：任务粒度依赖人工把控，初期我将多个页面合并为单任务，超出子智能体承载能力，根源是未吃透“单任务2–5分钟”规范。
2. **requesting-code-review**：通用检查项单人项目参考价值低，仅定向聚焦模块风险点的人工评审才有实质收益。

---

## 3. SDD 规约驱动开发工作流
### 3.1 标准执行链路
Brainstorming 输出 SPEC → writing-plans 生成 PLAN → subagent 实现 → 代码评审校验。所有子智能体无全局上下文，仅读取当前任务信息。

### 3.2 SDD 优缺点
**优点**
- 模块强隔离，Agent 仅依赖接口契约，不会产生意外耦合；
- worktree 支持多模块并行开发；
- 固定 SPEC/PLAN 下，更换智能体实现结果高度可复现。

**缺点**
- 每个子任务重复构建上下文，prompt 冗余；
- 多任务间无法自动统一共享数据结构，易出现接口不兼容；
- Agent 无历史经验记忆，同类错误重复出现。

### 3.3 SDD 与 TDD 的互补关系
课程明确 TDD 为硬性强制规范，标准循环为 RED（失败测试）→ GREEN（最小实现）→ REFACTOR（重构）。
SDD 负责顶层需求、架构拆分；TDD 约束单模块实现行为，二者层级不同、相辅相成，不存在冲突。
Superpowers 中 SDD 规划技能默认开启，`test-driven-development` 为独立可选技能。项目初期我未开启全局强制开关，全程采用「先编码、后补测试」的不合规流程；仅口头要求先写测试无约束力，Agent 仍优先产出业务代码。
虽后期补齐 387 个 Mock-LLM 测试，但丢失了 TDD 用例驱动设计、提前锁定边界的核心价值。若重做项目，会在 PLAN 中预定义测试用例，让子智能体先写 RED 测试再实现代码。

---

## 4. Subagent 工作流与任务拆分
### 4.1 智能体自主执行边界
- 简单单文件任务：可全程自主完成；
- 2–3 文件中等任务：60%–70% 进度易因存量数据结构理解偏差卡壳；
- 跨模块复杂页面任务：仅30%–40%就会设计出与规约冲突的逻辑。

### 4.2 最优任务粒度
单个核心业务文件+配套测试文件，单任务耗时10–15分钟。多文件合并的粗粒度任务几乎全部需要人工返工。

---

## 5. SPEC/PLAN 质量对实现的影响
### 5.1 正面案例：安全守卫系统
SPEC 完整定义入参、判定逻辑、输出枚举与异常分支，Agent 实现无偏差，一次通过评审。

### 5.2 反面案例：反馈校验与聚合模块
PLAN 未统一 `ValidationResult` 结构，前后任务 Agent 设计多套差异化返回格式，引发大量返工。

### 5.3 优化策略
后期统一规范：SPEC 锁定顶层接口，PLAN 嵌入共享数据结构代码片段，跨模块不兼容问题显著减少。

---

## 6. 高效 Prompt 工程方案
固定结构化模板收益最高，包含任务目标、文件清单、接口契约、验收标准、编码约束。前置统一接口契约能大幅减少接口不匹配、随意改动存量代码两类高频问题。

---

## 7. 凭据安全与分发工程收获
### 7.1 密钥安全
若无课程强制要求，我会直接使用明文环境变量。规范倒逼我搭建完整安全方案：对比钥匙串/.env 优劣、隐藏输入录入密钥、查看不回显明文、设计密钥更新清除流程，并完善威胁模型。

### 7.2 Docker 分发
容器要求推动落地多阶段构建、非root运行、运行时注入密钥、健康检查等生产规范，补齐完整交付链路。

---

## 8. 对 Superpowers 方法论的批判思考
### 8.1 核心假设验证
假设：标准化流程提升AI开发质量，整体成立。但两处假设不贴合单人开发：
1. 流程技能无法替代人工架构判断，任务粒度、跨模块约束必须人决策；
2. 子智能体仅适合既定接口实现，跨模块设计极易偏离规约。

### 8.2 方法论盲区
缺少三类标准化流程：故障根因调试、性能测量优化、技术债务分期重构框架。

### 8.3 Harness 项目适配性
整体适配度高，守卫、校验等边界清晰的机制适配子智能体开发；多模块联动场景仍需人工协调。

---

## 9. 重做项目的优化调整
1. 细化任务拆分，页面设计拆分为独立子任务；
2. PLAN 嵌入全部跨模块共享数据结构代码；
3. 项目初期搭建标准化 Prompt 模板；
4. SPEC+PLAN 定稿立刻执行冷启动验证；
5. 前端任务增加严格技术栈约束；
6. 初始化直接开启全局 TDD 强制，所有子任务自动走红-绿-重构循环。

---

## 10. 冷启动跨智能体验证实践
课程要求使用全新智能体、仅依靠 SPEC+PLAN 试运行，模拟同侪评审检验规约清晰度。
首版文档冷启动时，新 Agent 在多处隐性假设节点持续提问，暴露出数据结构、交互规则、依赖顺序等文档漏洞。两轮修订后，规约模糊带来的返工减少六成。
后续项目会将冷启动验证放在 PLAN 定稿后第一时间执行，前置消除文档缺陷。

---

## 11. TDD 流程偏离专项反思
### 11.1 偏离成因
项目初始化未通读课程规范，关闭全局 `test-driven-development` 开关，全程先编码后补测试。该独立技能可强制 Agent 先写测试，仅靠自然语言提示无法实现同等约束。

### 11.2 后置补测固有缺陷
初始387个测试，补救新增119个，总计506组，但存在底层短板：
1. 测试贴合已有代码，而非对齐需求，遗漏需求规定但未实现的边界场景；
2. 编码在先形成思维盲区，容易漏掉异常、幂等等反向用例；
3. 缺少前置测试约束，代码接口、异常逻辑设计松散。

### 11.3 落地补救措施
1. 全模块扩充边界、异常测试，CI unit-test Job 稳定全量通过；
2. 新增 `TDD_RECOVERY.md`，逐模块还原标准红-绿-重构流程；
3. 修订全部 PLAN，为所有任务补充 TDD 三步验证要求；
4. 配置 CLAUDE.md 全局强制 TDD，后续任务必须调用对应技能。

### 11.4 流程偏离带来的负面影响
1. 缺失原生 TDD 执行日志，补救文档证明效力不足；
2. 测试仅能验证代码，无法驱动模块化设计；
3. 缺少测试前置约束，部分模块耦合度高，测试编写繁琐。

---

## 12. 总结
1. 确定性机制是 Harness 核心：守卫、校验、状态机完全代码化，可脱离LLM运行506组独立测试，行为可预测；
2. AI 协作必须多层流程约束：SDD 管控顶层方向，TDD 约束单模块质量，冷启动验证提前修复规约漏洞，三者缺一不可；仅靠顶层规划会遗漏大量边界缺陷；
3. Superpowers 仅提供流程脚手架，任务拆分、规约定义、风险取舍、判断“做对与否”仍是工程师不可替代的核心工作。

---
*反思报告完 · 2026-08-13*
> AI 辅助润色说明：本文第 2、8 节的部分段落经 AI 辅助润色（语言流畅度和段落结构优化），全部案例、核心观点、项目实操经历均为作者本人独立思考产出。