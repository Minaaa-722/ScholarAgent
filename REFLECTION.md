# REFLECTION — 项目反思报告

> 约 2000 字 · 对本项目设计与开发过程的反思

---

## 1. 项目概览

ScholarAgent 是一个自编码的 Coding Agent Harness，用于自动生成 IEEEtran 会议格式的学术文献综述论文。项目从零开始，构建了一个包含状态机、工具系统、记忆系统、守卫系统、反馈闭环和 API 层的完整 Agent 框架，并在此基础上实现了学术综述生成这一具体应用。

---

## 2. 架构设计反思

### 2.1 分层架构的正确性

项目采用三层架构：**Web UI (React) → API (FastAPI) → Agent Harness (Python)**。这一选择的正确性在开发过程中得到验证：

- **关注点分离清晰**：前端负责展示和交互，API 负责协议转换，Harness 负责核心逻辑。三者可独立开发、测试和演進。
- **测试友好**：API 层可通过 `dependency_overrides` 注入 mock harness 进行测试，Harness 核心可通过 `MockLLM` 进行测试，前端可通过 mock API 进行测试。
- **扩展性**：未来可替换前端（如 CLI 版本）或替换后端（如切换 LLM 供应商），只需修改对应层。

### 2.2 状态机设计的取舍

状态机采用显式转换表，而非隐式状态转换。这一设计带来了明确的优势：

**优势**：
- 所有合法路径一目了然，便于审计
- 非法转换在运行时被拒绝，避免状态损坏
- 中断/恢复机制自然融入状态机

**不足**：
- 状态数增加时转换表规模线性增长
- 未来若引入并发子状态，当前设计无法直接支持

对于本项目的规模（10 个状态），显式转换表是最优选择。若项目扩展到数十个状态，应考虑使用状态模式（State Pattern）。

### 2.3 反馈闭环作为重点维度

选择反馈闭环作为深入维度（§A.4-D）是项目中最关键的架构决策之一。

**为什么是反馈闭环**：
- 学术综述对事实准确性要求极高，反馈闭环是质量保障的核心
- 5 个校验器天然适合用确定性代码实现，完美契合"机制是代码"要求
- 多轮迭代控制展示了 Agent 系统如何从错误中恢复

**实现质量**：
- 5 个校验器各司其职，覆盖引用、幻觉、字数、语言、连贯性 5 个维度
- `FeedbackAggregator` 提供综合评分，`RepairGenerator` 生成可执行修复指令
- Harness 的 `inject_feedback` 方法集成重试控制，最多 3 轮

**改进空间**：
- 当前校验器基于正则表达式，对语义理解有限
- 未来可引入 LLM 辅助的语义校验器，与确定性校验器互补
- 修复指令的自动执行（而非仅输出指令）值得进一步探索

---

## 3. 代码质量反思

### 3.1 测试覆盖

项目共 70 个测试，覆盖所有模块：

| 模块 | 测试数 | 覆盖率 |
|------|--------|--------|
| LLM 抽象层 | 4 | 核心路径 |
| 状态机 | 6 | 所有路径 |
| Harness 主循环 | 4 | 核心路径 |
| 工具系统 | 8 | 核心路径 |
| 守卫系统 | 10 | 所有路径 |
| 记忆系统 | 12 | 所有路径 |
| 反馈系统 | 23 | 所有路径 |
| API 层 | 5 | 核心路径 |

**值得肯定**：
- 所有测试可独立运行，不依赖真实 LLM
- 边界条件（空输入、重复数据、最大重试）均有覆盖
- API 测试使用 `httpx.AsyncClient` 模拟真实 HTTP 请求

**改进空间**：
- 测试覆盖率未精确测量（缺少 coverage 工具配置）
- 前端缺少自动化测试（如 React Testing Library）

### 3.2 DRY 原则

项目中存在良好的 DRY 实践：

- `_dedup_by_title` 函数被 `MergeResults` 和 `Dedup` 两个工具复用（`agent/tools/base.py:22`）
- `GuardrailVerdict` 枚举在 `base.py` 中定义，所有守卫复用
- `ValidationResult` 数据类在所有校验器间共享

也存在一些冗余：
- `OutputStandard` 和 `LanguagePolisher` 的非正式语言模式列表有部分重叠，但职责不同（守卫 vs 校验器），保留合理

### 3.3 错误处理

项目在以下方面有良好的错误处理：

- 状态机拒绝非法转换并抛出明确的 `ValueError`
- 校验器对空输入返回 `not passed` 而非崩溃
- `FormatBibtex` 对空标题有保护（`title or "Untitled"`）
- Harness 的 `inject_feedback` 在达到最大重试次数后优雅结束

---

## 4. 项目管理反思

### 4.1 任务拆分

9 个子任务的拆分基本合理，但存在以下可改进之处：

- **Task 6 和 Task 7 可合并**：校验器与其聚合/修复逻辑紧密耦合，分开开发导致需要修改同一个测试文件
- **Task 9 范围过大**：Web UI 的 5 个页面应拆分为 2-3 个任务，避免一次性提交过多代码

### 4.2 代码评审

每个任务完成后都进行了独立的代码评审，发现并修复了以下问题：

- `HallucinationDetector` 未使用的 `paper_ids` 参数（1e7777c）
- `LanguagePolisher` 正则模式中多余的空格导致匹配失败（1e7777c）
- `FormatBibtex` 空标题崩溃（4968b7e）
- `Dedup` 和 `MergeResults` 去重逻辑重复，提取为共享函数（4968b7e）
- `OutputStandard` 正则模式中多余空格（a7e0be2）
- `OpSafety` 未使用变量（a7e0be2）

这些修复展示了代码评审的价值——在代码进入主线前捕获了 6 个真实问题。

### 4.3 交付物完整性

项目最终检查发现 6 个计划内的交付物缺失（Dockerfile、README.md、AGENT_LOG.md、REFLECTION.md、SPEC_PROCESS.md、CI 配置），原因是在 9 个子任务中未将其列为具体步骤。这提醒我们在任务拆分时，应将非代码交付物纳入任务列表。

---

## 5. 技术选型反思

### 5.1 FastAPI 的选择

**正确**：异步支持、自动 OpenAPI 文档、WebSocket 原生集成、Pydantic 模型校验，所有这些特性都在项目中得到利用。

### 5.2 React + TypeScript 的选择

**正确**：TypeScript 的类型系统在 API 客户端代码中减少了运行时错误。Vite 的开发体验优于 Webpack。

### 5.3 SQLite 的选择

**正确**：零配置、单用户场景足够、Python 标准库内置。`PersistentMemory` 在测试中使用 `:memory:` 数据库，无需额外清理。

### 5.4 Mock-LLM 测试策略

**正确决策**：通过 `MockLLM` 的固定响应和固定工具调用，所有 Harness 逻辑可脱离真实 LLM 验证，测试速度快（70 个测试在 0.6 秒内完成）。

---

## 6. 未来工作

### 6.1 近期（1-2 周）

- 集成真实 LLM（OpenAI / Anthropic）并验证端到端流程
- 补充 Web UI 的 3 个骨架页面（AgentExecution、KnowledgeExplorer、FinalReview）的完整实现
- 添加 `coverage` 工具配置，精确测量测试覆盖率

### 6.2 中期（1-2 月）

- 为 ShellExec 工具添加真实沙箱执行环境
- 实现 PDF 下载和解析的真实集成（PyMuPDF）
- 添加 Docker Compose 配置，简化部署

### 6.3 长期（3 月+）

- 支持多并发任务
- 添加基于 LLM 的语义校验器，增强反馈闭环
- 支持更多输出格式（如 ACL、ICML 模板）
- 构建论文关系图的可视化组件

---

## 7. 总结

ScholarAgent 项目成功构建了一个自编码的 Coding Agent Harness，覆盖了决策封装、工具、记忆、治理、反馈、配置六个维度，并以学术综述生成为具体应用场景进行了验证。项目共 70 个测试全部通过，9 个子任务按计划完成，代码经过独立评审。

**三个关键收获**：

1. **确定性机制的价值**：在 Agent 系统中，将尽可能多的机制（校验器、守卫、状态转换）用确定性代码实现，可以显著提高系统的可靠性和可测试性。

2. **分层架构的重要性**：清晰的关注点分离（UI / API / Harness）使得各层可独立开发、测试和演进，降低了系统的整体复杂度。

3. **代码评审的必要性**：6 个在评审中发现并修复的问题证明，即使经过 TDD 开发，代码评审仍然能捕获底层设计问题。

---

*反思报告完 · 2026-07-26*